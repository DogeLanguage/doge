# Release notes

Drafted GitHub release bodies, one file per version. Paste the matching file into
the GitHub release for its tag once the tag is pushed (the `compare/...` link
resolves only after the tag exists). Style mirrors past releases: a doge-speak
tagline, grouped highlights with issue links, an install line, and a full-changelog
compare link. See the Releases section of [../CLAUDE.md](../CLAUDE.md) for the
tag-driven release flow itself.

| Version | Notes |
|---|---|
| v0.3.1 | [v0.3.1.md](v0.3.1.md) — extended numerics, `Bytes`, `hunt`/`chase` modules, wider `fetch` |
