/target

# Local scratch script kept in the repo root for quick experiments.
/test.doge

# Built VS Code extension packages — a release artifact, not source.
*.vsix

# Codex planner→executor handoff artifact — per-task scratch, not source.
/.codex/plan.md
