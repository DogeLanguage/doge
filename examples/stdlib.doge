so nerd
so strings

bark nerd.sqrt(16)
bark nerd.min(3, 2.5)
bark nerd.round(2.5)
bark nerd.pi
bark strings.beeg("wow")
bark strings.split("a,b,c", ",")
bark strings.join(["much", "wow"], " ")
bark strings.index("kabosu", "bos")
bark strings.index("kabosu", "zzz")
wow
